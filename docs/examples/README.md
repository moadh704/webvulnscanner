# Sample scan outputs

Real reports generated by WebVulnScanner against **local lab targets only**
(DVWA / bWAPP). Paths and secrets sanitized for the public repo.

| File | Target | Notes |
|------|--------|--------|
| `sample-report-dvwa.*` | `http://localhost/dvwa` | Full hybrid scan, 60 findings (13 Critical / 25 High / 19 Medium / 3 Low), no AI |
| `sample-report-dvwa-ai.*` | `http://localhost/dvwa` | Same target with AI review active — 46 findings (7 Critical / 25 High / 11 Medium / 3 Low), 14 false positives dismissed, verdicts + remediation per finding |
| `sample-report-dvwa-full-ai.*` | `http://localhost/dvwa` | All 6 modules with live OpenRouter free-tier AI — 48 findings (7 Critical / 19 High / 19 Medium / 3 Low), 5 verified, 8 false positives dismissed by AI, AI remediations across sqli/xss/cmdi/idor |
| `sample-results-dvwa.md` | `http://localhost/dvwa` | Readable findings table from the same run |
| `sample-report-bwapp-xss.*` | `http://localhost/bWAPP/xss_get.php` | Focused reflected XSS + AI remediation |

Open the `.html` files in a browser. Use the `.json` files for tooling demos.